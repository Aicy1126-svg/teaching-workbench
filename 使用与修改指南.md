# 教培备考工作台 · 使用与修改指南

> 给使用者/后续维护者看的一页纸：怎么启动、怎么当 App 装到手机、要备份什么、想改功能改哪个文件。

---

## 一、怎么启动（电脑端）

1. 双击 **`启动工作台.bat`**（在项目根目录）。
2. 它会自动启动本地服务器，并弹出浏览器打开 `http://localhost:8080`。
3. 第一次打开会让你**填自己的账号和密码**（第一次自动注册），之后这台电脑自动登录。
4. 手机/平板想同步：连同一 WiFi，浏览器打开 `http://电脑IP:8080`，用**同一个账号**登录即可。

⚠️ 千万不要直接双击 `index.html` 打开（那样是 `file://` 协议，无法联网同步）。
⚠️ 关闭那个黑色命令行窗口 = 停止服务；要再用就重新双击 `启动工作台.bat`。

---

## 二、怎么当 App 装到手机/平板

点右上角 `👤 账号·同步` → **📱 安装到手机/平板**，里面有按系统（iOS / Android）的步骤图。要点：

- **iPhone / iPad（Safari）**：分享 → 添加到主屏幕。局域网 HTTP 即可，无需 HTTPS。点开就是全屏 App。
- **Android（Chrome）**：菜单 → 安装应用 / 添加到主屏幕。
  - 注意：安卓要“真·独立 App（无地址栏）”需要 **HTTPS**；用 `http://电脑IP` 打开时可能只生成书签（点开仍在浏览器）。要真独立可让电脑走 HTTPS 或公网隧道。
- 前提：电脑端 `启动工作台.bat` 已启动，且手机/平板与电脑同一 WiFi。

---

## 三、需要保存 / 备份什么（最重要）

整个项目就是一个文件夹 **`teaching-workbench/`**，换电脑时把整个文件夹拷走即可。里面要特别留意这几样：

| 要备份的东西 | 位置 | 说明 |
|---|---|---|
| **账号 + 云端同步数据** | `sync-server/data/`（users.json、data_*.json） | 换电脑必带！没有它就得重新注册、且云端数据丢失。 |
| **代码本身** | 整个 `teaching-workbench/` | 你所有的功能修改都在这里。 |
| **备课 PDF/Word 文档、手写图** | 浏览器 **IndexedDB**（本机）+ 云端账号同步 | ✅ **已随账号同步**（v2026-07-28 起）。登录同一账号，文档库会自动上传到服务器、在其它设备自动拉回。清浏览器后用同一账号登录可恢复。 |
| **排课 / 学生 / 便利贴** | 浏览器 localStorage + 云端 | 已同步到服务器，登录同一账号可在任意设备恢复。 |

> 简单记：**代码整个文件夹带走；`sync-server/data/` 千万别漏；备课文档现在随账号云同步，换电脑不用再手动导出（但特别重要的资料仍建议本地留一份）。**

---

## 三·a 换电脑了怎么办（明确步骤）

已部署 Railway 公网地址后，换电脑影响很小，但有两样必须提前备份：

**不受影响（云端已有，登录即回）：**
- 账号、排课、学生、便利贴等文本数据 → 在 Railway 云端。新电脑打开 `https://teaching-workbench-sync-production.up.railway.app` 用同一账号登录即恢复。

**会丢、必须提前备份：**
1. **备课 PDF/Word 文档、手写图** —— 从 v2026-07-28 起**已随账号云同步**，换电脑登录同一账号会自动拉回，一般不用手动导出。但为防止云端异常，特别重要的资料仍建议用「⬇️ 导出」存一份到 U 盘/云盘。
2. **`sync-server/data/`**（本地账号+云端数据双保险） → 拷走。

**建议整体备份：**
3. 整个 `teaching-workbench/` 文件夹（代码 + 配置都在，GitHub/Railway 也有，但本地留一份最稳）。

**新电脑恢复：**
1. 浏览器打开公网地址 → 同一账号登录 → 文本数据自动回来。
2. 文档库：登录同一账号后点 `🔄 立即同步`，云端文档库会自动拉回（PDF/Word/手写图都恢复）。无需再手动导入。
3. （可选本地跑）把文件夹拷到新电脑，双击 `启动工作台.bat`。

> 关键提醒：只要**公网地址 `fixed_url.txt` 指向的 Railway 项目不删除**，换任何电脑都不影响使用。

---

## 四、想改功能时，改哪个文件

项目是纯 HTML/CSS/JS（零框架），文件都在 `teaching-workbench/` 下。

| 想改的内容 | 改哪里 |
|---|---|
| **排课表（教师视图 HTML 表格）样式、颜色、空时段压缩** | `js/app.js` → `renderTeacherScheduleView()` |
| **学生视图 / 家长版排课图（Canvas 图片、空时段压缩、整块填色）** | `js/app.js` → `renderStudentScheduleView()` + `renderScheduleToCanvas()` |
| **科目显示颜色** | `js/data.js` → `getSubjectColor()`；并在设置面板里调 |
| **学生色板颜色** | `js/data.js` → `getStudentColor()` |
| **登录 / 注册 / 云同步逻辑** | `js/login.js`、`js/sync.js` |
| **同步服务器（端口、账号存储）** | `sync-server/server.js`（端口改 `PORT` 常量） |
| **整体界面样式** | `css/style.css` |
| **备课文档库（PDF/Word 导入导出、手写备课）** | `js/prep.js`（存浏览器 IndexedDB） |
| **文档库「按学生 + 上课时间」分类、筛选、归类弹窗** | `js/prep.js` → `renderDocsTab()` / `refreshDocFiltersAndList()` / `openImportTagsModal()` / `tagDoc()` / `saveDocsWithTags()`；分类辅助 `getStudentsList()` / `getStudentClassSlots()` |
| **App 安装 / PWA 图标** | `manifest.json`、`sw.js`、`index.html` 头部、`icons/` |

---

## 四·a 文档库分类（按学生 + 上课时间）

文档库支持给每份 PDF/Word 打「学生」和「上课时间」两个标签，并在列表顶部按这两个维度筛选。

**怎么用**
- 导入时：选好文件后弹窗让你选「关联学生」和「关联上课时间」（也可留空，之后在卡片上点「🏷 归类」补打）。
- 列表顶部：「分类筛选」两个下拉——学生（来自学生名单）、上课时间（来自该学生的排课时段，取并集保证能筛到）。点「重置」清空。
- 卡片上会显示分类标签；「🏷 归类」可随时修改某份文档的分类。

**数据存在哪**：每份文档在 IndexedDB 里多了两个字段 `student`（学生名）和 `classTime`（如「周一 09:00-11:00 · 数学」）。都在本机浏览器，并**随文档库整体一起云同步到账号**（v2026-07-28 起）；导出/导入也保留这两个字段。

**想改分类逻辑改哪**
- 学生下拉来源：`getStudentsList()`（读 `students` 名单）。
- 上课时间下拉来源：`getStudentClassSlots(name)`（读 `schedule` 排课时段，格式 `周X HH:mm-HH:mm · 科目`）。
- 筛选与渲染：`refreshDocFiltersAndList()`。
- 导入归类弹窗：`openImportTagsModal()` + `confirmImportTags()` + `saveDocsWithTags()`。
- 已有文档补打标签：`tagDoc()` + `confirmTagDoc()`。
- 筛选器 / 标签样式：`css/style.css` 的 `.doc-filters`、`.doc-badge`、`.doc-card-tags`。

---

## 五、改完怎么让它生效

1. **改了服务器代码**（`sync-server/server.js`）：关闭命令行窗口，重新双击 `启动工作台.bat`。
2. **改了前端代码**（`js/*.js`、`css/*.css`、`index.html`）：在浏览器里**硬刷新**（Ctrl+F5 / Cmd+Shift+R），强制跳过缓存。
   - 服务 Worker 已升到 `v9`，会自动拉取最新静态文件；若仍异常可在浏览器开发者工具 → Application → Service Workers → 点 “Unregister” 再刷新。
3. 手机/平板同步：在 App 里点 `🔄 立即同步`。

---

## 四·b 脱离 IP / 其他网络也能正常使用（离线优先）

核心数据（排课、学生、便利贴、AI 记录）都存**浏览器 localStorage**，原本就不需要服务器也能看、能改。
之前的阻塞点：打开时会强制弹登录框、同步失败会中断使用。**现已改为离线优先**：

- **不连原服务器 / 换网络 / 服务器没开**：打开工作台照常使用本地数据，不再弹登录框、不再卡住。右上角状态点显示灰色「未同步」。
- **想同步时**：主动点右上角 `☁️ 登录同步` 才发起连接。连不上就静默保持离线，不报错打扰。
- **当 App 用（iOS 添加到主屏幕 / Android 安装）**：本机数据随时可用，只是"跨设备同步"需要设备能连到运行 `启动工作台.bat` 的那台电脑。

> 也就是说：**任何网络下都能正常用工作台本身**；只有"跨手机/平板同步"才依赖那台电脑的局域网地址（或 Railway 公网）。文档库（PDF/Word/手写图）存在本机 IndexedDB，任何网络下可用；**登录同一账号后文档库也会自动上传到云端，换设备登录即可拉回**（v2026-07-28 起）。

**想改这块逻辑改哪**
- 离线优先 / 不强制登录：`js/login.js` → `initSync()`（已去掉强制弹框与失败重登）。
- 同步请求与静默降级：`js/sync.js`（`_request` / `syncNow` 已是 try-catch 容错）。
- 若要让"跨设备同步"彻底摆脱单台电脑：需要把 `sync-server` 部署到公网（云服务器 / 内网穿透），再把 `js/sync.js` 的 `serverUrl` 指向公网地址（在登录框选"自定义服务器"填入）。这是进阶需求，非必须。

---

## 四·c 手机端适配（方块按键 + 页面固定）

- **周切换按钮改方块**：`css/style.css` 的 `@media (max-width:768px)` 里，`schedule-weekbar` 改为 4 列等高方块网格（上周/下周/本周/从上周复制），不再是一条长条。
- **主页面固定不晃动**：同媒体查询里加了 `html,body/.app-layout/.main-content { overflow-x:hidden; width:100% }`，消除横向溢出导致的左右滑动；AI 面板在手机端为底部抽屉，不挤占主内容宽度。

---

## 四·d 排课表「是否上课」用配色标注（主色块=学生色）

排课表（教师视图 HTML）课程块**主色块用学生色**（整块背景），上课状态用**左上角彩色圆点 + 文字**标注，与导出图片的状态色图例完全一致：

| 状态 | 圆点/文字配色 | 图例 |
|---|---|---|
| 待上课 | 绿 `#6A9B5A` | 🟢 |
| 已上完 | 灰 `#9AA09A` | ⚪ |
| 调课 | 黄 `#C8A040` | 🟡 |
| 请假 | 红 `#B07080` | 🔴 |

学生身份 = 整块背景色（学生色板）；状态 = 块内彩色圆点 + 状态文字。两者一眼区分。

**想改颜色改哪**
- 状态配色：`js/app.js` 的 `renderTeacherScheduleView()` 内 `statusDot` / `statusText` 映射。
- 学生色背景：同函数里 `background:${studentColor}`（来自 `js/data.js` 的 `getStudentColor()`）。
- 样式类：`css/style.css` 的 `.slot-*`（仅语义占位，不再覆盖背景）。
- 导出图片里的状态色：`js/app.js` 的 `renderScheduleToCanvas()` 中 `statusColors` 与底部图例文字。

---

## 四·e 怎么杜绝「手机端服务器停止响应」

手机端连的是那台电脑的局域网服务器，**电脑关闭/睡眠/断网/断电**时手机就"停止响应"。两层防护：

**① 前端已极致健壮（页面永不卡死）**
- 离线优先：未登录/网络不通/服务器没开，工作台照常用本机数据，不弹框、不阻塞。
- 请求 3 秒超时（`js/sync.js` 的 `_request` 用 `AbortController`）：手机端连不上时 3 秒即降级为离线，绝不白屏转圈。
- 登录失败提示改为"可先离线使用本机数据"，不再粗暴报错。

**② 减少服务器被误关（已改 `.bat`）**
- `启动工作台.bat` 现以**最小化后台**方式启动服务器（`start /min`），并提示"可直接关闭本窗口，服务器继续运行"。关闭黑窗口 ≠ 停止服务。
- 彻底停止：任务管理器结束 `node.exe` 进程。

**③ 根治方案：让同步服务器"永远在线"（进阶，非必须）**
单台电脑终归会关。要手机端任何时间都不依赖那台电脑，需把 `sync-server` 部署到公网：
- 最简单：用内网穿透（如 `cloudflared tunnel`、`localtunnel`、花生壳）把本机 8080 暴露为公网 HTTPS 地址；
- 或把 `sync-server/` 整个文件夹传到任意云服务器/云函数运行。
- 拿到公网地址后，在登录框选"自定义服务器"填入，手机端在任何网络下都能同步。
- 注：纯前端（排课/学生/便利贴/文档库）仍本机可用，只有"跨设备同步"才需要这一步。

> 一句话：**现在手机端即使服务器停了也只是"不能跨设备同步"，工作台本身照常用、不卡死**；想要"永远能同步"就把服务器搬到公网。

---

## 四·f 课时结算对账单（可填老师/单价、自动算总价）

课时结算模块现在支持**手动填入授课老师和单价，总价自动计算生成**：

- 顶部两个输入框：**授课老师**、**单价（元/课时）**，按「学生 + 月份」记忆（下次打开自动带出）。
- **总价自动计算**：总价 = 总课时 × 单价，实时显示在设置区，并写入「对账单预览」「复制文本」「导出图片」。
- 数据存 `billingSettings`（localStorage，随账号云同步）。

**想改这块改哪**
- 输入框 + 自动计算 UI：`js/app.js` → `Modules.billing`（含 `saveBillingSetting()` 实时刷新总价）。
- 对账单文本/图片里的老师名与总价：`js/app.js` 的 `billText` 拼接、`exportBillingImage()`。
- 数据存取：`DB.get('billingSettings', {})` / `DB.set('billingSettings', ...)`。

---

## 四·g 「永远能同步」：公网服务器入口已显眼化

登录框里新增「🔧 高级：用公网服务器实现"永远能同步"」折叠区，可直接填入公网同步地址（如 `https://你的公网地址:8080`），点「保存服务器地址」即生效。

- 实现：`js/login.js` 的 `showLoginModal()`（折叠输入）+ `applyServerUrl()` + `doSyncLogin()` 登录前先应用地址。
- 底层：`js/sync.js` 的 `serverUrl`（同源留空；填了就走公网）。
- 部署公网的方法见四·e 第③点（内网穿透 / 云服务器）。部署好后把地址填进这里，手机/平板在任何网络下都能同步。

---

## 四·h 已替你部署好永久固定公网同步（Railway）

已用 **Railway** 把 `sync-server` 部署到公网，拿到**永久固定地址**：

```
https://teaching-workbench-sync-production.up.railway.app
```

- 手机/平板在任何网络下打开该地址 → 用**同一账号**登录 → 自动同步。
- 地址只要不删除 Railway 项目就**永久不变**。
- 电脑端：已把该地址写入 `sync-server/fixed_url.txt`，`启动工作台.bat` 启动本地页面时，同步会自动走这个公网地址。

> 说明：为了让公网地址能直接打开工作台页面（而不是只能 API 同步），我已把前端 `index.html/css/js/icons/manifest.json/sw.js` 复制到 `sync-server/public/`，并修复 `server.js` 优先 serve 该目录。

---

## 四·i 杜绝"打不开 / 白屏"的三道防线

之前"老是打不开"主要是：① SW 旧缓存卡白屏；② 服务器进程被关/崩溃。现已加三道防线：

1. **Service Worker 自愈**（`index.html` 内联防自检）：
   - 若核心 JS 4 秒内未初始化 → 自动带 `?nocache` 重开，并注销旧 SW 彻底绕过坏缓存。
   - `js/app.js` 的 `init()` 末尾设 `window.__appReady = true` 标记启动成功。
2. **服务器崩溃自动重启 + 异常保护**（`sync-server/server.js` + `启动工作台.bat`）：
   - `server.js` 加了 `uncaughtException` / `unhandledRejection` 保护，异常不退出。
   - `.bat` 用 `for /L` 循环包住启动，进程退出 3 秒后自动重启；`start /min` 后台常驻，关黑窗口 ≠ 停服务。
3. **开机自启（建议）**：把 `启动工作台.bat` 的快捷方式放进「启动」文件夹（shell:startup），电脑开机即自动跑服务。

> 终极兜底：若仍白屏，手动浏览器地址栏加 `?nocache=1`（如 `http://localhost:8080/?nocache=1`）即可强制 bypass 缓存重开。

---

## 四·j 固定公网地址（部署到云，无需 Cloudflare）

想要**永久不变**的公网地址，最稳的是把 `sync-server` 部署到支持 Node.js 的云平台（Railway / Render / 自己的服务器）。**都不需要 Cloudflare 账号**。

- 配置已备好：`railway.json`、`render.yaml`、`.gitignore`、`DEPLOY.md`（逐步图文）。
- server.js 已支持 `process.env.PORT`（云平台自动注入），`0.0.0.0` 监听。
- 部署后拿到固定域名（如 `xxx.up.railway.app`），手机/平板任何网络打开该域名、用同一账号即同步。

**部署后切换成本机/固定地址**：
- 在 `sync-server/fixed_url.txt` 写入你的固定公网地址（一行），`启动工作台.bat` 下次启动就**不再拉临时隧道**，直接用固定地址。删掉该文件则恢复临时穿透。
- 手机端直接打开固定域名即可，登录框"高级"里服务器地址留空（同源自动同步）。

> 说明：Railway/Render 需你用自己的账号注册并连接代码仓库（GitHub），这一步需你操作；其余配置均已就绪，连仓库即部署。若你有自己的云服务器，见 DEPLOY.md 方式三。

---

## 六、目录速览

```
teaching-workbench/
├── index.html              # 入口页面（含 PWA 元信息）
├── manifest.json           # App 安装配置
├── sw.js                   # 离线缓存 Service Worker（v11）
├── css/style.css           # 全部样式
├── js/
│   ├── data.js             # 数据模型 + 颜色函数(getSubjectColor/getStudentColor)
│   ├── app.js              # 各功能模块（排课/学生/成绩/对账/素材…）
│   ├── login.js            # 登录/注册/同步菜单/安装指引
│   ├── sync.js             # 跨设备云同步客户端
│   └── prep.js             # 备课文档库（IndexedDB）
├── sync-server/
│   ├── server.js           # 合并服务器：托管网页 + 同步 API（端口 8080，含崩溃保护）
│   ├── cloudflared.exe     # 内网穿透工具（自动生成公网地址）
│   ├── public_url.txt      # 当前公网同步地址（手机/平板用）
│   ├── fixed_url.txt       # 可选：写入固定公网地址后跳过临时隧道
│   ├── DEPLOY.md           # 部署到 Railway/Render/云服务器 步骤
│   ├── railway.json        # Railway 部署配置
│   ├── render.yaml         # Render 部署配置
│   ├── .gitignore          # 部署时忽略 data/log/cloudflared.exe
│   ├── 部署教程-无脑版.md   # 图文步骤（注册GitHub→建仓库→Railway部署）
│   ├── 部署上传.bat        # 一键上传到GitHub（填用户名/token/仓库名）
│   ├── DEPLOY.md           # 部署到 Railway/Render/云服务器 步骤
│   ├── log/                # 运行日志
│   └── data/               # 账号与云端数据（要备份！）
├── icons/                  # App 图标
└── 启动工作台.bat          # 一键启动（后台常驻+自愈重启+自动穿透）
```

---

## 七、常见问题

- **打不开 / 登录失败**：先确认 `启动工作台.bat` 已双击启动；别用 `file://` 直接开 `index.html`。
- **手机连不上**：手机和电脑要在同一 WiFi；用电脑的局域网 IP（如 `http://192.168.x.x:8080`，启动窗口会显示）。
- **换电脑后资料没了**：把旧电脑的 `sync-server/data/` 整个拷到新电脑对应位置，再用同一账号登录即可恢复云端数据。备课文档/手写图从 v2026-07-28 起已随账号云同步，新电脑登录同一账号点 `🔄 立即同步` 即可拉回；特别重要的资料仍建议本地导出留底。
- **想改默认端口 8080**：改 `sync-server/server.js` 里的 `PORT`，保存后重启。
- **备课文档怎么换设备恢复**：登录同一账号后，文档库会随同步自动上传/拉回（PDF/Word/手写图都在内）。原理：同步时 `js/sync.js` 把 IndexedDB 里的文档转 base64 推到服务器 `data_docs_*.json`，另一台设备登录后自动写回 IndexedDB。
- **文档同步失败/卡住**：文档较大时可能超时（当前未设单文件上限，建议单份 PDF/Word 不超过约 20MB）。若失败，状态点会显示红色，可重试同步；本地数据不会丢。
