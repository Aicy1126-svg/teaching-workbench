# 教培备考工作台 · 项目接手说明（给 AI / 接手开发者）

> 本文件用于：换电脑后把本项目交给 AI 助手（或新开发者）继续完善功能时，让其快速理解项目结构与修改切入点。
> 配套文件：`使用说明书.md`（给最终用户）、`README.md`（部署与分发）、`教培工作台_完整存档.html`（单文件总览）。

---

## 0. 一句话定位
纯前端（HTML/CSS/JS）的教学效率工具，**无构建步骤**，由一个 Node 服务（`sync-server/server.js`）同时托管前端静态文件与云同步接口 `/api/*`。数据默认存在浏览器 `localStorage`，登录后通过 `/api/*` 在云端（PostgreSQL 或本地文件）多端同步。已加 PWA（可"添加到主屏幕"当 App）与轻付费激活码门槛。

## 1. 技术栈
- 前端：原生 HTML + CSS + JavaScript（**无框架、无打包器、无 npm 前端依赖**）。
- 后端：`sync-server/server.js`（Node 内置 `http`，无 Express）。
- 存储：
  - 云端模式（设环境变量 `DATABASE_URL` 指向 PostgreSQL）：每个用户一行 JSON。
  - 本地模式（**不设 `DATABASE_URL`**）：每个用户一个文件 `data_<username>.json`，存在 server 运行目录。桌面/分发默认用此模式，买家数据各自独立。
- PWA：`manifest.json` + `sw.js`（network-first 缓存策略）。
- 激活码：`js/activation.js`（纯 JS HMAC-SHA256 校验）+ `js/activation.config.js`（SECRET 与收款码配置）。

## 2. 目录结构
```
teaching-workbench/
├── index.html                 # 入口；内联启动自检 + 注册 Service Worker + 引入脚本
├── css/style.css              # 全部样式；含手机端 @media(max-width:768px) 2×2 布局
├── js/
│   ├── data.js                # 数据层 DB（localStorage 封装，所有读写入口）
│   ├── sync.js                # 云同步客户端（调用 /api/*，含同步风暴修复）
│   ├── login.js               # 登录/注册逻辑，成功后调用个性化与重渲染
│   ├── app.js                 # 主应用：全部 17 个模块渲染 + 启动入口 Activation.check(init)
│   ├── prep.js                # 预约/课前准备相关
│   ├── activation.js          # 激活码门槛（HMAC 校验 + 全屏拦截页）
│   └── activation.config.js   # SECRET、PAYMENT_QR、MERCHANT_TIP 配置
├── manifest.json              # PWA 清单
├── sw.js                      # Service Worker（network-first）
├── icons/                     # icon-192.png / icon-512.png / 及占位 payment-qr.png
├── sync-server/
│   ├── server.js              # 后端：托管前端 + /api/register|login|pull|push|logout
│   └── public/                # 实际被 server 托管的前端副本（部署用，需与根目录同步）
├── public/                    # 另一份前端副本（备用同步目录）
├── tools/gen-codes.js         # 激活码生成工具：node tools/gen-codes.js <数量> [前缀]
├── 使用说明书.md              # 给最终用户
├── README.md                  # 给分销商/部署者
└── 项目接手说明.md            # 本文件
```
> ⚠️ 重要：Railway / 本地 `node sync-server/server.js` 实际托管的是 **`sync-server/public/`** 下的文件。改完 `index.html`/`js/*` 后，务必 `cp` 同步到 `sync-server/public/`（和 `public/`）再部署，否则线上不生效。

## 3. 本地运行
```bash
cd teaching-workbench
node sync-server/server.js          # 默认端口见 server.js（如 3000/8899）
# 浏览器打开 http://localhost:<端口>
```
- 本地多端同步：开多个浏览器标签/设备访问同一地址并登录同一账号即可。
- 端口可通过环境变量/命令行参数调整（见 server.js 顶部）。

## 4. 部署（Railway 一键）
- 把整个仓库推到 GitHub，Railway 连仓库、Root Directory 设为 `teaching-workbench`，Start Command 设为 `node sync-server/server.js`。
- 加 PostgreSQL 插件并把它生成的 `DATABASE_URL` 设为环境变量（可选；不设则用本地文件存储）。
- 改代码后 `git push` 即自动部署；但**前端改动要同步进 `sync-server/public/`**（见上）。

## 5. 关键文件职责速查（改功能从这里下手）
| 想改什么 | 改哪里 |
|----------|--------|
| 新增/修改功能模块 | `js/app.js` 的 `Modules.<name>.render / bindEvents / init`；侧栏项在 `renderSidebar` 附近 |
| 数据字段 / 存储结构 | `js/data.js`（DB 封装）；同步字段统一前缀 `teaching_workbench_` |
| 同步逻辑 / 同步风暴 | `js/sync.js`（已修：push 仅在数据变化时更新时间戳；pull 仅在数据真变时 reload） |
| 登录 / 注册 | `js/login.js` |
| 主题 / 颜色 / 手机布局 | `css/style.css`（手机 2×2 见 `@media(max-width:768px)` `.grid-4`） |
| 激活码门槛 / 收款码 | `js/activation.js` + `js/activation.config.js` |
| 后端接口 / 存储 | `sync-server/server.js` |
| PWA 图标 / 名称 | `manifest.json` + `icons/`（替换 icon-192/512.png） |
| 生成激活码给买家 | `node tools/gen-codes.js <数量> [前缀]`（需先 `cd` 到项目根） |

## 6. 启动流程（排查"白屏/刷新"必看）
`index.html` 内联脚本：
1. 若 URL 带 `?nocache` → 注销所有 Service Worker 再跳回，用于清坏缓存。
2. 页面 `load` 后 4 秒若 `window.__appReady` 仍为 false → 跳 `?nocache`（**白屏兜底**）。
3. `load` 后注册 `sw.js`（PWA）。
`app.js` 的 `DOMContentLoaded` → 立即 `window.__appReady = true` → `Activation.check(init)`：未激活弹激活页（不调 init），已激活直接 `init()`。
> ⚠️ 历史坑：曾因激活页不调 init 导致 `__appReady` 恒 false → 4 秒自检无限跳 `?nocache` 表现为"一直刷新"。已在 `app.js` 与 `activation.js` 的 `showScreen` 开头都置 `__appReady=true` 修复（commit `eca217b`）。

## 7. 已知待完善（用户可能让你继续做的）
1. **App 图标未定稿**：当前 `icons/` 是占位图标，用户想要蝴蝶/小猫风格（已多轮尝试 AI 生成，未选定）。改 `manifest.json` + 替换 `icons/icon-192.png`、`icon-512.png`。
2. **收款码需手动放**：把微信/支付宝收款码截图命名为 `payment-qr.png` 覆盖到 `icons/payment-qr.png` 即生效；未放则激活页显示占位框。
3. **激活为轻门槛**：SECRET 明文在 `activation.config.js`，非强加密。换 SECRET 后须重新生成码并重新部署（线上与分发包都用同一 SECRET）。
4. **同步风暴**：已修，但若再出现"疯狂刷新/同步"，优先查 `js/sync.js` 的 `push/pull/_pullOnly` 时间戳与 `location.reload` 触发条件。
5. **多端激活状态**：激活状态按设备存于 `localStorage`（一码可激活多台自有设备）；清缓存/换浏览器需重输。

## 8. 数据备份与恢复（用户换电脑双保险）
- 用户侧：应用内「🎨 个性化设置 → 💾 数据备份与迁移 → 📤 导出全部数据」得一个 JSON；换电脑后「📥 导入备份」恢复。
- 云端：登录同一账号自动同步（Railway PostgreSQL 或本地文件）。
- 若要把用户数据一并交 AI 完善：让用户导出 JSON，连同本项目 ZIP 一起提供。

## 9. 激活码（作者自用）
- 作者自用激活码：`AUTHOR · T72Q-7DVG-XVZO-HOV5-5BAQ-NYA`（基于当前 SECRET 生成，有效）。
- 批量生成：`node tools/gen-codes.js 30 AICY` 生成 30 个可售卖码。
- 校验逻辑：`activation.js` 的 `verify()` 用 `ACTIVATION_CONFIG.SECRET` 做 HMAC-SHA256，取前 6 字节与码内 mac 比对。

---
© 教学工作台 · 项目接手说明
